import Layout from '@components/common/Layout';
import { Intro, MusicVideo } from '@components/home';

const Home = () => {
  return (
    <Layout>
      <Intro />
      <MusicVideo />
    </Layout>
  );
};

export default Home;
